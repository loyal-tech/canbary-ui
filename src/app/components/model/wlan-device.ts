export interface IWlanDevice {
  deviceId: number;
  deviceName: string;
  groupId: string;
  syncStatus: string;
  serialNumber: string;
  macAddress: string;
  ipAddress: string;
  lattitude: string;
  longitude: string;
}
